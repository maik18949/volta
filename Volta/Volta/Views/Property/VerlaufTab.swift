import SwiftUI

struct VerlaufTab: View {
    let property: Property

    var body: some View {
        VStack {
            Spacer()
            Text("Verlauf — coming soon")
                .font(.appBody)
                .foregroundStyle(Color.appSecondaryText)
            Spacer()
        }
    }
}
