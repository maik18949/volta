import SwiftUI

struct ImmobiliendatenTab: View {
    let property: Property

    var body: some View {
        VStack {
            Spacer()
            Text("Immobiliendaten — coming soon")
                .font(.appBody)
                .foregroundStyle(Color.appSecondaryText)
            Spacer()
        }
    }
}
